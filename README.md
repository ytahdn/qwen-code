# Web Shell context usage UI evidence

Source commit: `85bf4c196d59c2c1ab311e477504704ac6ad4dce`.

Actual Chromium screenshots from the local Web Shell with a mock daemon.
Expanded/collapsed snapshots and the default details panel were visually inspected.
These are UI evidence, not real backend/Git verification. Host callback routing is
covered by component integration tests, not these browser screenshots.

本分支仅保存素材。截图来自使用模拟 daemon 的本地 Web Shell Chromium E2E，
已目视检查展开/收起状态和默认详情面板；不代表真实后端或 Git 操作验证。
宿主回调由组件集成测试验证，不由这些截图证明。
